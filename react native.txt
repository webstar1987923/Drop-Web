 we need a react native module that will allow us to ping an IP of our choice and get the time taken.
 You may use promises for the callback.

https://github.com/img-more/react-native-ping/invitations
you’ll be building a standalone library that can be added to any project

import Ping from ‘react-native-ping’;

 Ping.ping(‘192.0.0.1’).then((data) => console.log(data));
where data would be a JSON object with information about the ping